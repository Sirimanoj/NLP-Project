services:
  - type: web
    name: graph-ir-streamlit
    env: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r graph_ir_app/requirements.txt
    startCommand: streamlit run graph_ir_app/app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
